import styled from "styled-components";
import { LoginForm, LoginPageContainer } from "../Login/Login.styled";

const RegisterPageContainer = styled(LoginPageContainer)``;

const RegisterForm = styled(LoginForm)``;

export { RegisterPageContainer, RegisterForm };
